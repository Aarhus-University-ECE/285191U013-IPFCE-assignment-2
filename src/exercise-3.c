#include <stdio.h>

// Write a program to compute the Fibonacci numbers. Your program
// should take an integer n as input from the user, and it then calculates and prints
// each Fibonacci number from the first Fibonacci number to the nth. For example, a
// typical execution where the user inputs n = 7 could be:

int main() {

    // printf("Hello, World!\n");
    // int input;
    // scanf("%d", &input);
    
    return 0;
}
