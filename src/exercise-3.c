#include <stdio.h>

// Write a program to compute the Fibonacci numbers. Your program
// should take an integer n as input from the user, and it then calculates and prints
// each Fibonacci number from the first Fibonacci number to the nth. 

int main() {

    // printf("Hello, World!\n");
    // int input;
    // scanf("%d", &input);
    
    return 0;
}
